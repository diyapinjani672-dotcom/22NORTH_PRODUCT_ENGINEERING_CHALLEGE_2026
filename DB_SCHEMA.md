# Database Schema

SQLite for local dev (`skyjet.db`, generated by `services/seed_data.py`);
same models work against Postgres by changing `DATABASE_URL`. Defined in
`backend/models/models.py` with SQLAlchemy.

```mermaid
erDiagram
    PASSENGER ||--o{ BOOKING : has
    FLIGHT ||--o{ BOOKING : "operates"
    FLIGHT ||--o{ DISRUPTION : "affected by"
    BOOKING ||--o{ REBOOKING : "recovered via"
    BOOKING ||--o{ REFUND : "refunded via"
    BOOKING ||--o{ NOTIFICATION : receives
    BOOKING ||--o{ CHAT_MESSAGE : "chats about"
    FLIGHT ||--o{ REBOOKING : "rebooked onto"

    PASSENGER {
        string id PK
        string first_name
        string last_name
        string email
        string phone
        string frequent_flyer_tier
        string frequent_flyer_number
        string emergency_contact_name
        string emergency_contact_phone
        string preferred_seat
        string preferred_meal
        string language
    }

    FLIGHT {
        string id PK
        string flight_number
        string airline
        string origin_code
        string origin_city
        string destination_code
        string destination_city
        datetime scheduled_departure
        datetime scheduled_arrival
        datetime estimated_departure
        datetime estimated_arrival
        enum status
        string gate
        string terminal
        string aircraft_type
        int duration_minutes
        int stops
        int seats_available
    }

    BOOKING {
        string id PK
        string pnr UK
        string passenger_id FK
        string flight_id FK
        string seat_number
        string cabin_class
        float fare_amount
        string fare_type
        datetime booking_date
        string status
    }

    DISRUPTION {
        string id PK
        string flight_id FK
        string type
        string reason
        string reason_detail
        int delay_minutes
        datetime announced_at
    }

    REBOOKING {
        string id PK
        string original_booking_id FK
        string new_flight_id FK
        enum status
        int recovery_score
        float fare_difference
        datetime created_at
    }

    REFUND {
        string id PK
        string booking_id FK
        float amount
        string refund_type
        enum status
        int estimated_processing_days
        string reason
        datetime created_at
    }

    NOTIFICATION {
        string id PK
        string booking_id FK
        string type
        string title
        string message
        datetime created_at
        bool read
    }

    CHAT_MESSAGE {
        string id PK
        string booking_id FK
        string role
        text content
        datetime created_at
    }
```

## Notes

- IDs are prefixed UUIDs (`pax_`, `flt_`, `bkg_`, `dis_`, `rbk_`, `rfd_`,
  `ntf_`, `msg_`) for readability in logs and API responses.
- `Booking.pnr` is unique and is the passenger-facing identifier used at
  login, alongside `Passenger.last_name`.
- `Flight.status` is an enum (`ON_TIME`, `DELAYED`, `CANCELLED`, `DIVERTED`,
  `BOARDING`, `DEPARTED`, `LANDED`) driving both the UI status badge and the
  disruption-detection logic.
- `Rebooking.recovery_score` is computed once at rebook time and stored so
  the confirmation screen and future analytics don't need to recompute it.
- No payment tables exist by design — refunds are simulated per the
  constraint of no payment integration (see `ASSUMPTIONS.md`).
